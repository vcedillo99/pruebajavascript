function View (container) {
    return {
        init () {
            let cont = document.getElementById("content")
            console.log(cont.offsetHeight, cont.offsetWidth);
        }
    }
}

window.on('', (event) => {
    
})
